/**
 * webmos — Run MOS models (DNSMOS) in the browser via ONNX Runtime Web.
 *
 * @packageDocumentation
 */

export { initDNSMOS, runDNSMOS } from "./dnsmos.js";
export type { DNSMOSOptions, DNSMOSResult } from "./dnsmos.js";
